/// <reference types="vite/client" />
/*
 * Los tipos que Vite le da al proyecto: `import.meta.env`, y —lo que acá hace
 * falta— la declaración de `*?raw`, que es como `el-css-esta-entero.test.ts`
 * lee el CSS portado como texto.
 */
